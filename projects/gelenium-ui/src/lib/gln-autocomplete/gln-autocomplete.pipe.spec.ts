import { GlnAutocompletePipe } from './gln-autocomplete.pipe';

describe('GlnAutocompletePipe', () => {
  it('create an instance', () => {
    const pipe = new GlnAutocompletePipe();
    expect(pipe).toBeTruthy();
  });
});
