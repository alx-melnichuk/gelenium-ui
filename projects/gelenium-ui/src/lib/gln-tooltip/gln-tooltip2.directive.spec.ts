import { GlnTooltip2Directive } from './gln-tooltip2.directive';

describe('GlnTooltip2Directive', () => {
  it('should create an instance', () => {
    const directive = new GlnTooltip2Directive();
    expect(directive).toBeTruthy();
  });
});
