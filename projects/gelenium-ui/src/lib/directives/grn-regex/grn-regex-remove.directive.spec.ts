import { GrnRegexRemoveDirective } from './grn-regex-remove.directive';

describe('GrnRegexRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnRegexRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
