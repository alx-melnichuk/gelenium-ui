import { GlnAutoFocuseDirective } from './gln-auto-focuse.directive';

describe('GlnAutoFocuseDirective', () => {
  it('should create an instance', () => {
    const directive = new GlnAutoFocuseDirective();
    expect(directive).toBeTruthy();
  });
});
