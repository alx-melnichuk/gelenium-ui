import { GlnFrameSizeDirective } from './gln-frame-size.directive';

describe('GlnFrameSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new GlnFrameSizeDirective();
    expect(directive).toBeTruthy();
  });
});
