import { GlnRegexRemoveDirective } from './gln-regex-remove.directive';

describe('GlnRegexRemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new GlnRegexRemoveDirective();
    expect(directive).toBeTruthy();
  });
});
