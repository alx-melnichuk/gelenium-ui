import { GlnRegexCheckDirective } from './gln-regex-check.directive';

describe('GlnRegexCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new GlnRegexCheckDirective();
    expect(directive).toBeTruthy();
  });
});
