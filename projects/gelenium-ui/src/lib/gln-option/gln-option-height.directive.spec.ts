import { GlnOptionHeightDirective } from './gln-option-height.directive';

describe('GlnOptionHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new GlnOptionHeightDirective();
    expect(directive).toBeTruthy();
  });
});
