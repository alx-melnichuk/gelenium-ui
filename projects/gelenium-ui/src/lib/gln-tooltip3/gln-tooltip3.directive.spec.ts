import { GlnTooltip3Directive } from './gln-tooltip3.directive';

describe('GlnTooltip3Directive', () => {
  it('should create an instance', () => {
    const directive = new GlnTooltip3Directive();
    expect(directive).toBeTruthy();
  });
});
