import { GrnRegexMatchDirective } from './grn-regex-match.directive';

describe('GrnRegexMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnRegexMatchDirective();
    expect(directive).toBeTruthy();
  });
});
