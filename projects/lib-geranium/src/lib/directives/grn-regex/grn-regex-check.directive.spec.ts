import { GrnRegexCheckDirective } from './grn-regex-check.directive';

describe('GrnRegexCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnRegexCheckDirective();
    expect(directive).toBeTruthy();
  });
});
