import { GrnOrnamentDirective } from './grn-ornament.directive';

describe('GrnOrnamentDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnOrnamentDirective();
    expect(directive).toBeTruthy();
  });
});
