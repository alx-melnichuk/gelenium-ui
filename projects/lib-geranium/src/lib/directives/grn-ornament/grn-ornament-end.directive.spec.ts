import { GrnOrnamentEndDirective } from './grn-ornament-end.directive';

describe('GrnOrnamentEndDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnOrnamentEndDirective();
    expect(directive).toBeTruthy();
  });
});
