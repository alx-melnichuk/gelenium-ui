import { GrnFrameLabelDirective } from './grn-frame-label.directive';

describe('GrnFrameLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnFrameLabelDirective();
    expect(directive).toBeTruthy();
  });
});
