import { GrnAutoFocuseDirective } from './grn-auto-focuse.directive';

describe('GrnAutoFocuseDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnAutoFocuseDirective();
    expect(directive).toBeTruthy();
  });
});
