import { GrnSizeDirective } from './grn-size.directive';

describe('GrnSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnSizeDirective();
    expect(directive).toBeTruthy();
  });
});
