import { GrnFrameSizeDirective } from './grn-frame-size.directive';

describe('GrnFrameSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new GrnFrameSizeDirective();
    expect(directive).toBeTruthy();
  });
});
